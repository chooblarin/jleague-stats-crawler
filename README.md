# J League Stats Crawler

This is a small script of web crawler for J league data site [*](https://data.j-league.or.jp).

## Usage

```
deno run --unstable --allow-run --allow-env --allow-read --allow-net --allow-write main.ts
```

or

```
./main.ts
```
